#include <stdio.h>
  int main(){
    int a,b,c;
    int emp;
    scanf("%d %d %d\n",&a,&b,&c);

    if(a>b){
      emp = b;
      b = a;
      a = emp;
    }
    if(b>c){
      emp = c;
      c = b;
      b= emp;
    }
    if(a>b){
      emp = b;
      b = a;
      a = emp;
    }
    printf("%d %d %d\n",a,b,c);
    return 0;

  }
